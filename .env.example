# PostgreSQL connection string (pooler — used at runtime)
DATABASE_URL=""
# Direct connection — used by Prisma migrations only (bypasses pgBouncer)
DIRECT_URL=""
SUPABASE_API_KEY=""

# Google Workspace SMTP
SMTP_USER=""
SMTP_PASS=""

# Admin auth (generate a strong random password and secret)
ADMIN_PASSWORD=""
SESSION_SECRET=""

# App URL (used for sitemap and Open Graph)
NEXT_PUBLIC_APP_URL=""

# OpenRouter AI (used by the chatbot widget)
OPENROUTER_API_KEY=""

# Analytics (optional)
NEXT_PUBLIC_GA_ID=""
NEXT_PUBLIC_CLARITY_ID=""
